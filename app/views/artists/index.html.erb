<div class="container">

<p id="notice"><%= notice %></p>

<div class="name"><h1>Listing Artists</h1></div>

<% @artists.each do |artist| %>
    <div class="name"><h3><%= link_to "#{artist.name}", artist %></h3></div>
    <div class="thumbnail"><%= link_to image_tag(artist.img), artist %></div>
    <!-- <td><%= link_to 'Show', artist %></td> -->
    <!-- <td><%= link_to 'Edit', edit_artist_path(artist) %></td> -->
    <!-- <td><%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
<% end %>

<div class="name"><%= link_to 'New Artist', new_artist_path, class: "btn btn-default btn-lg" %></div>
</div>
